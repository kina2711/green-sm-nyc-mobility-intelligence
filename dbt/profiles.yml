green_sm_nyc:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: data/warehouse/mobility.duckdb
      threads: 2
      settings:
        memory_limit: "2GB"
        preserve_insertion_order: false

